<!DOCTYPE html>
<html>
  <head>
    <title>FullstackOpentable</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <% if current_user %>
      <script>
        window.currentUser = { 
          "id": <%= current_user.id %>,
          "email": "<%= current_user.email %>"
          };
      </script>
    <% end %>

  </head>

  <body>
    <%= yield %>
  </body>
</html>
